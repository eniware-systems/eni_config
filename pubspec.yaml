name: eni_config
description: "Flutter configuration management system with YAML/JSON support, environment-specific configs, and flexible provider architecture."
version: 2.0.7
homepage: https://eniware-systems.de
publish_to: https://gitea.eniware-systems.de/api/packages/eni-flutter/pub

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter

  eni_svc:
    git:
      url: https://github.com/eniware-systems/eni_svc.git
      ref: 2.0.7
  eni_utils:
    git:
      url: https://github.com/eniware-systems/eni_utils.git
      ref: 2.0.4
  yaml: ^3.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
